.edit__user-account__title
  Edit account
.edit__user-account__form
  = form_for(current_user) do |f|
    .form-field
      .form-field__label
        = f.label :名前
      .form-field__input
        = f.text_field :name, autoforcus: true, class: "form-field__name"
    .form-field
      .form-field__label
        = f.label :email
      .form-field__input
        = f.email_field :email, class: "form-field__email"
    .form-field__submit
      = f.submit "更新する", class: "update__user-account__submit solid"
.return__mypage-link
  = link_to user_path(current_user), class: "registered__content-link" do
    戻る