.edit__user__information__title
  アカウントの編集
.edit__user__information__form
  = form_for(current_user) do |f|
    .form-field
      .form-field__label
        = f.label :name
      .form-field__input
        = f.text_field :name, autoforcus: true
    .form-field
      .form-field__label
        = f.label :email
      .form-field__input
        = f.email_field :email
    .form-field__submit
      = f.submit "更新する", class: "update__user-account__submit"
.return__mypage-link
  = link_to user_path(current_user), class: "registered__content-link" do
    戻る